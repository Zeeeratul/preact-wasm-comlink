import { Button } from "./ui/button";

export default function MemoryTable() {
  const handlePress = async () => {};

  return (
    <div className="w-full h-full bg-red-400">
      <Button onClick={handlePress}>Cc</Button>
    </div>
  );
}
