import Memory from "./memory";

export default class PPU {
  constructor(memory: Memory) {
    // Initialisation du PPU avec une référence à la mémoire
  }

  getNumber(): u8 {
    return 0;
  }

  // ... autres méthodes et propriétés du PPU ...
}
